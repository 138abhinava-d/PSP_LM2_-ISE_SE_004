// to add two numbers//
#include<stdio.h>
int main()
{
 int a, b ,sum;
  printf("enter the valuue of the two numbers which have to be added: ");
    scanf("%d%d ", &a, &b);
  sum=a+b;
  printf("the sum of thetwo numbers which you have entered is= %d ", sum);
  return 0;
}
  
